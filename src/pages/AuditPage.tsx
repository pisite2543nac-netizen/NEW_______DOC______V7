import {useEffect,useState} from 'react';import {supabase} from '../lib/supabase';import {fmtDate} from '../lib/utils';export function AuditPage(){const[rows,setRows]=useState<any[]>([]);useEffect(()=>{supabase.from('audit_logs').select('*,profiles(full_name)').order('created_at',{ascending:false}).limit(300).then(({data})=>setRows(data||[]))},[]);return <><div className="page-title"><h1>Audit log</h1></div><div className="table-wrap"><table><thead><tr><th>เวลา</th><th>ผู้ทำ</th><th>Action</th><th>Entity</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{fmtDate(r.created_at)}</td><td>{r.profiles?.full_name||'-'}</td><td>{r.action}</td><td>{r.entity_type} {r.entity_id||''}</td></tr>)}</tbody></table></div></>}
