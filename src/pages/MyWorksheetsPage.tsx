import {useEffect,useState} from 'react';import {Link} from 'react-router-dom';import {api} from '../services/api';import {fmtDate} from '../lib/utils';import type {Worksheet} from '../types/domain';export function MyWorksheetsPage(){const[items,setItems]=useState<Worksheet[]>([]);useEffect(()=>{api.worksheets(false).then(setItems)},[]);return <><div className="page-title"><h1>ใบงานของฉัน</h1></div><div className="cards">{items.map(w=><div className="card" key={w.id}><span className="badge">{w.mode}</span><h3>{w.title}</h3><p className="muted">{w.subjects?.code} {w.subjects?.name}</p><p>กำหนด: {fmtDate(w.due_at)}</p><Link className="btn primary" to={`/submit/${w.id}`}>ทำใบงาน</Link></div>)}</div></>}
