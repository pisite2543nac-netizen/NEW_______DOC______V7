import {Navigate} from 'react-router-dom';import {useAuth} from '../contexts/AuthContext';import {Loading} from './Loading';export function ProtectedRoute({admin=false,children}:{admin?:boolean;children:JSX.Element}){const{loading,session,profile}=useAuth();if(loading)return <Loading/>;if(!session)return <Navigate to="/login" replace/>;if(admin&&profile?.role!=='admin')return <Navigate to="/" replace/>;return children}
